<%= simple_form_for(@profile_picture) do |f| %>
  <%= f.error_notification %>

  <div class="form-inputs">
    <%= f.input :image %>
    <%= f.association :card %>
  </div>

  <div class="form-actions">
    <%= f.button :submit %>
  </div>
<% end %>
