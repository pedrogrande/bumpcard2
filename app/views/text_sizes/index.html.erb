<h1>Listing text_sizes</h1>

<table>
  <tr>
    <th>Size</th>
    <th></th>
    <th></th>
    <th></th>
  </tr>

<% @text_sizes.each do |text_size| %>
  <tr>
    <td><%= text_size.size %></td>
    <td><%= link_to 'Show', text_size %></td>
    <td><%= link_to 'Edit', edit_text_size_path(text_size) %></td>
    <td><%= link_to 'Destroy', text_size, method: :delete, data: { confirm: 'Are you sure?' } %></td>
  </tr>
<% end %>
</table>

<br />

<%= link_to 'New Text size', new_text_size_path %>
