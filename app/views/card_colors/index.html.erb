<h1>Listing card_colors</h1>

<table>
  <tr>
    <th>Card color</th>
    <th>Card</th>
    <th>Active</th>
    <th></th>
    <th></th>
    <th></th>
  </tr>

<% @card_colors.each do |card_color| %>
  <tr>
    <td><%= card_color.card_color %></td>
    <td><%= card_color.card %></td>
    <td><%= card_color.active %></td>
    <td><%= link_to 'Show', card_color %></td>
    <td><%= link_to 'Edit', edit_card_color_path(card_color) %></td>
    <td><%= link_to 'Destroy', card_color, method: :delete, data: { confirm: 'Are you sure?' } %></td>
  </tr>
<% end %>
</table>

<br />

<%= link_to 'New Card color', new_card_color_path %>
