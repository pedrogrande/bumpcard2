<%= simple_form_for(@card_font_alignment) do |f| %>
  <%= f.error_notification %>

  <div class="form-inputs">
    <%= f.input :alignment %>
    <%= f.association :card %>
  </div>

  <div class="form-actions">
    <%= f.button :submit %>
  </div>
<% end %>
