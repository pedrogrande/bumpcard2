<h1>Listing text_positions</h1>

<table>
  <tr>
    <th>Position</th>
    <th></th>
    <th></th>
    <th></th>
  </tr>

<% @text_positions.each do |text_position| %>
  <tr>
    <td><%= text_position.position %></td>
    <td><%= link_to 'Show', text_position %></td>
    <td><%= link_to 'Edit', edit_text_position_path(text_position) %></td>
    <td><%= link_to 'Destroy', text_position, method: :delete, data: { confirm: 'Are you sure?' } %></td>
  </tr>
<% end %>
</table>

<br />

<%= link_to 'New Text position', new_text_position_path %>
